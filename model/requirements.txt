# Python dependencies for the model training/evaluation pipeline.
# Install with:  pip install -r model/requirements.txt
#
# Version notes (Python 3.12): TensorFlow 2.16 requires numpy < 2.0, so scipy and
# scikit-learn are capped to the last releases that still support numpy 1.x — otherwise
# TensorFlow crashes importing a numpy-2-only scipy.

tensorflow>=2.16,<2.17
# tf-keras + TF_USE_LEGACY_KERAS=1 keeps the Keras 2 API (ImageDataGenerator) on TF 2.16.
tf-keras

pillow>=10.0
numpy>=1.26,<2.0

# Evaluation only (step 6): loss curves + confusion matrix. Pinned for numpy 1.x support.
matplotlib>=3.7
scipy>=1.11,<1.13
scikit-learn>=1.3,<1.5
